package produce.consume.old;

import java.util.ArrayList;
import java.util.List;

public class ProducerConsumerExample {

    public static void main(String[] args) {

        /*List<Integer> sharedList=new ArrayList<>();
        Thread threadProduce=new Thread(new Produce(sharedList,7),"PRODUCE");
        Thread threadProduce2=new Thread(new Produce(sharedList,7),"#####: ");
        Thread threadConsume=new Thread(new Consume(sharedList),"CONSUME");

        threadProduce.start();
        threadConsume.start();
        threadProduce2.start();*/


    }
}
